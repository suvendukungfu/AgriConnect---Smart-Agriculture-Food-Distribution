apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agriconnect-ingress
  namespace: agriconnect
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: api.agriconnect.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-gateway
                port:
                  number: 8080
