# payment-service

Domain microservice for payments.

## Run locally
1. Copy .env.example to .env
2. npm install
3. npm run dev

## Endpoints
- GET /health
- GET /api/payments
- POST /api/payments/events
