# notification-service

Domain microservice for notifications.

## Run locally
1. Copy .env.example to .env
2. npm install
3. npm run dev

## Endpoints
- GET /health
- GET /api/notifications
- POST /api/notifications/events
